{
  "name": "deep-loop",
  "version": "1.22.1",
  "type": "module",
  "description": "Loop Engineering control plane over the deep-suite",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "test:unit": "node scripts/run-unit-tests.mjs",
    "validate": "node scripts/deep-loop.mjs validate",
    "preflight": "npm run validate && npm test",
    "eval:fixture": "node scripts/eval-deep-loop.mjs --mode fixture",
    "eval:report": "node scripts/eval-deep-loop.mjs --mode fixture --report"
  }
}
